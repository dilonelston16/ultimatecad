/* Milestone 1.5.1 — real license tests and mobile sidebar */
.mobile-sidebar-toggle,
.mobile-sidebar-close,
.mobile-sidebar-backdrop,
.mobile-page-brand {
  display: none;
}

.written-test-action {
  width: 100%;
  margin: 10px 0 12px;
}

.practical-test-note {
  margin: 10px 0 12px;
  padding: 10px 12px;
  border: 1px solid #725b20;
  border-radius: 9px;
  background: #2b2411;
  color: #e6c868;
  font-size: 11px;
  line-height: 1.5;
}

.written-test-page {
  display: grid;
  gap: 18px;
  max-width: 920px;
  margin: 0 auto;
}

.written-test-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 24px;
  padding: 24px;
  border: 1px solid var(--line);
  border-radius: 18px;
  background: linear-gradient(120deg, #0b1a31, #10284a);
}

.written-test-header h2 {
  margin: 7px 0 3px;
  font-size: 25px;
}

.written-test-header p {
  margin: 0;
  color: #79a9df;
}

.written-progress-copy {
  min-width: 100px;
  text-align: right;
}

.written-progress-copy strong,
.written-progress-copy span {
  display: block;
}

.written-progress-copy strong {
  font-size: 25px;
}

.written-progress-copy span {
  color: var(--muted);
  font-size: 11px;
}

.written-progress-track {
  height: 7px;
  overflow: hidden;
  border-radius: 999px;
  background: #101f35;
}

.written-progress-track span {
  display: block;
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, #278cff, #755bff);
  transition: width .2s ease;
}

.written-question-card {
  padding: 30px;
}

.question-number {
  color: #65a9fb;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: .08em;
  text-transform: uppercase;
}

.written-question-card h2 {
  margin: 12px 0 24px;
  font-size: clamp(22px, 3vw, 31px);
  line-height: 1.3;
}

.written-options {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}

.written-options button {
  min-height: 78px;
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 15px;
  border: 1px solid var(--line);
  border-radius: 13px;
  background: #081529;
  color: var(--text);
  text-align: left;
  cursor: pointer;
}

.written-options button:hover {
  border-color: #387fca;
  background: #0d203b;
}

.written-options button.selected {
  border-color: #278cff;
  background: #102d55;
  box-shadow: inset 0 0 0 1px rgba(39,140,255,.3);
}

.written-options b {
  width: 35px;
  height: 35px;
  flex: none;
  display: grid;
  place-items: center;
  border-radius: 9px;
  background: #132540;
  color: #76b7ff;
}

.written-test-nav {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  margin-top: 26px;
}

.written-test-result,
.written-test-empty {
  max-width: 680px;
  margin: 40px auto;
  padding: 45px;
  border: 1px solid var(--line);
  border-radius: 22px;
  text-align: center;
  background: #09172c;
}

.written-test-result > svg,
.written-test-empty > svg {
  width: 54px;
  height: 54px;
  margin-bottom: 16px;
}

.written-test-result.passed {
  border-color: #256747;
  background: linear-gradient(145deg, #0b2a22, #09172c);
}

.written-test-result.passed > svg {
  color: #43dd98;
}

.written-test-result.failed {
  border-color: #73333d;
  background: linear-gradient(145deg, #32151c, #09172c);
}

.written-test-result.failed > svg,
.written-test-empty > svg {
  color: #ff7182;
}

.written-test-result h2 {
  margin: 10px 0;
  font-size: 52px;
}

.written-test-result p,
.written-test-empty p {
  max-width: 520px;
  margin: 0 auto 24px;
  color: var(--muted);
  line-height: 1.7;
}

.written-result-actions {
  display: flex;
  justify-content: center;
  gap: 10px;
}

@media (max-width: 700px) {
  .app-shell {
    display: block;
    min-height: 100vh;
  }

  .workspace {
    min-width: 0;
    width: 100%;
    padding-top: 58px;
  }

  .mobile-sidebar-toggle {
    position: fixed;
    top: 10px;
    left: 12px;
    z-index: 120;
    width: 42px;
    height: 42px;
    display: grid;
    place-items: center;
    border: 1px solid #263d5d;
    border-radius: 11px;
    background: rgba(8, 20, 38, .96);
    color: white;
    box-shadow: 0 12px 30px rgba(0,0,0,.35);
  }

  .mobile-sidebar-backdrop {
    position: fixed;
    inset: 0;
    z-index: 128;
    display: block;
    border: 0;
    background: rgba(0, 6, 16, .7);
    backdrop-filter: blur(3px);
  }

  .sidebar {
    position: fixed !important;
    top: 0;
    bottom: 0;
    left: 0;
    z-index: 130;
    width: min(88vw, 330px) !important;
    height: 100dvh !important;
    max-height: none !important;
    overflow-y: auto;
    transform: translateX(-105%);
    transition: transform .22s ease;
    box-shadow: 20px 0 60px rgba(0,0,0,.45);
  }

  .sidebar.mobile-open {
    transform: translateX(0);
  }

  .sidebar-brand {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .mobile-sidebar-close {
    width: 38px;
    height: 38px;
    flex: none;
    display: grid;
    place-items: center;
    border: 1px solid #263d5d;
    border-radius: 10px;
    background: #0b1a30;
    color: white;
  }

  .sidebar-section {
    display: grid;
    grid-template-columns: 1fr !important;
  }

  .sidebar-section .section-label,
  .sidebar-section .department-switch {
    grid-column: auto !important;
  }

  .shell-page-header {
    padding: 16px 16px 18px 68px;
  }

  .mobile-page-brand {
    display: none;
  }

  .written-test-header {
    align-items: flex-start;
    flex-direction: column;
  }

  .written-progress-copy {
    text-align: left;
  }

  .written-question-card {
    padding: 20px 16px;
  }

  .written-options {
    grid-template-columns: 1fr;
  }

  .written-test-nav,
  .written-result-actions {
    align-items: stretch;
    flex-direction: column;
  }

  .written-test-nav .button,
  .written-result-actions .button {
    width: 100%;
  }
}
