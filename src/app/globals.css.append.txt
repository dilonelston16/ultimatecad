/* Sidebar visibility and navigation fix */
.sidebar-brand .brand > span:last-child {
  display: grid;
  gap: 2px;
}

.sidebar-brand .brand small {
  display: block;
  color: #657a96;
  font-size: 9px;
  font-weight: 600;
  letter-spacing: 0;
}

.nav-item {
  width: 100%;
}

.nav-item span {
  flex: 1;
}

.nav-item small {
  margin-left: auto;
  border: 1px solid #263b5d;
  border-radius: 999px;
  padding: 2px 6px;
  color: #6f849f;
  font-size: 8px;
  text-transform: uppercase;
  letter-spacing: .06em;
}

.nav-item-disabled {
  cursor: default;
  opacity: .62;
}

.nav-item-disabled:hover {
  background: transparent;
  color: #a9b9ce;
}

.sidebar-signout {
  border: 0;
  background: transparent;
  cursor: pointer;
}

.sidebar-signout:hover {
  background: rgba(255,77,98,.1);
  color: #ff8494;
}

.shell-page-header {
  padding: 24px 28px;
  border-bottom: 1px solid var(--line);
  background: rgba(7,16,31,.92);
}

.shell-page-header h1 {
  margin: 0;
  font-size: 24px;
}

.shell-page-header p {
  margin: 6px 0 0;
  color: var(--muted);
}

@media (max-width: 900px) {
  .app-shell {
    grid-template-columns: 210px minmax(0, 1fr);
  }

  .sidebar {
    padding-left: 10px;
    padding-right: 10px;
  }
}

@media (max-width: 700px) {
  .app-shell {
    display: block;
  }

  .sidebar {
    position: relative;
    width: 100%;
    height: auto;
    max-height: none;
  }

  .sidebar-section {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .sidebar-section .section-label,
  .sidebar-section .department-switch {
    grid-column: 1 / -1;
  }

  .sidebar-bottom {
    margin-top: 18px;
  }
}
