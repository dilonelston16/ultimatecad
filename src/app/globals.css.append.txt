/* Milestone 1.6 — DMV completion */
.dmv-admin-page,
.dmv-admin-stack {
  display: grid;
  gap: 20px;
}

.dmv-admin-controls,
.point-control {
  margin: 16px 0;
}

.dmv-admin-controls label,
.point-control label {
  display: grid;
  gap: 8px;
  color: var(--muted);
  font-size: 11px;
}

.dmv-admin-controls textarea {
  min-height: 88px;
  resize: vertical;
}

.point-control input {
  max-width: 140px;
}

.dmv-record-list {
  display: grid;
  gap: 12px;
}

.dmv-record-list > article {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  padding: 16px;
  border: 1px solid var(--line);
  border-radius: 13px;
  background: #08162a;
}

.dmv-record-list > article > div:first-child {
  display: grid;
  gap: 5px;
}

.dmv-record-list span {
  color: var(--muted);
  font-size: 11px;
}

.dmv-action-row,
.dmv-action-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.button.danger {
  border-color: #74343d;
  background: #3a1820;
  color: #ff9aa6;
}

.license-management-copy {
  min-width: 230px;
}

.dmv-action-grid button {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 8px 10px;
  border: 1px solid #294567;
  border-radius: 8px;
  background: #0d2039;
  color: #d7e7f9;
  cursor: pointer;
}

.dmv-action-grid button:hover {
  border-color: #3889dd;
  background: #12305a;
}

@media (max-width: 760px) {
  .dmv-record-list > article {
    align-items: stretch;
    flex-direction: column;
  }

  .dmv-action-row,
  .dmv-action-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }

  .dmv-action-row .button,
  .dmv-action-grid button {
    width: 100%;
    justify-content: center;
  }
}

@media (max-width: 460px) {
  .dmv-action-row,
  .dmv-action-grid {
    grid-template-columns: 1fr;
  }
}
