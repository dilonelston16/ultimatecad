"use client"; import {useState} from "react"; import {useRouter} from "next/navigation"; import styles from "./marketplace.module.css";
export default function C({character,items}:any){const r=useRouter(),[open,setOpen]=useState(false),[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),res=await fetch('/api/marketplace',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({characterId:character.id,listingType:f.get('listingType'),title:f.get('title'),description:f.get('description'),price:f.get('price')})}),b=await res.json();if(!res.ok)return setMsg(b.error);setOpen(false);r.refresh()}return <div className={styles.page}><section className={styles.hero}><div><span>Community classifieds</span><h2>{items.length} active listings</h2><p>List vehicles, properties, businesses, items, services and jobs.</p></div><button onClick={()=>setOpen(!open)}>Create listing</button></section>{msg&&<div className={styles.message}>{msg}</div>}{open&&<form className={styles.form} onSubmit={submit}><div className={styles.formGrid}><label>Type<select name="listingType"><option value="vehicle">Vehicle</option><option value="property">Property</option><option value="business">Business</option><option value="item">Item</option><option value="service">Service</option><option value="job">Job</option></select></label><label>Title<input name="title" required/></label><label>Price<input name="price" type="number" min="0" required/></label><label className={styles.full}>Description<textarea name="description"/></label></div><button>Publish listing</button></form>}<section className={styles.grid}>{items.length?items.map((x:any)=>{const s=Array.isArray(x.seller)?x.seller[0]:x.seller;return <article className={styles.card} key={x.id}><code>{x.listing_number}</code><h3>{x.title}</h3><p>{x.description||'No description'}</p><dl><div><dt>Price</dt><dd>${Number(x.price).toLocaleString()}</dd></div><div><dt>Seller</dt><dd>{s?.first_name} {s?.last_name}</dd></div></dl></article>}):<div className={styles.empty}>No active listings.</div>}</section></div>}
