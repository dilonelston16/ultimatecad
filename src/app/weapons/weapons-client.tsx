"use client"; import {useState} from "react"; import {useRouter} from "next/navigation"; import styles from "./weapons.module.css";
export default function C({character,items}:any){const r=useRouter(),[open,setOpen]=useState(false),[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),res=await fetch('/api/weapons',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({characterId:character.id,weaponType:f.get('weaponType'),make:f.get('make'),model:f.get('model'),caliber:f.get('caliber'),notes:f.get('notes')})}),b=await res.json();if(!res.ok)return setMsg(b.error);setOpen(false);r.refresh()}return <div className={styles.page}><section className={styles.hero}><div><span>Registered equipment</span><h2>{items.length} weapons</h2><p>Serial numbers and registrations are generated automatically.</p></div><button onClick={()=>setOpen(!open)}>Register weapon</button></section>{msg&&<div className={styles.message}>{msg}</div>}{open&&<form className={styles.form} onSubmit={submit}><div className={styles.formGrid}><label>Type<input name="weaponType" required/></label><label>Make<input name="make" required/></label><label>Model<input name="model" required/></label><label>Caliber<input name="caliber"/></label><label className={styles.full}>Notes<textarea name="notes"/></label></div><button>Register</button></form>}<section className={styles.grid}>{items.length?items.map((x:any)=><article className={styles.card} key={x.id}><code>{x.registration_number}</code><h3>{x.make} {x.model}</h3><p>{x.weapon_type} · {x.caliber||'Caliber not set'}</p><dl><div><dt>Serial</dt><dd>{x.serial_number}</dd></div><div><dt>Status</dt><dd>{x.status}</dd></div></dl></article>):<div className={styles.empty}>No registered weapons.</div>}</section></div>}
