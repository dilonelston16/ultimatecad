"use client";
import { ArrowLeft, BadgeCheck, LoaderCircle, Save } from "lucide-react";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { FormEvent, useState } from "react";

export default function CharacterForm(){const router=useRouter();const [saving,setSaving]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);setError('');const fd=new FormData(e.currentTarget);const body=Object.fromEntries(fd.entries());const r=await fetch('/api/characters',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const data=await r.json();if(!r.ok){setError(data.error??'Unable to create character');setSaving(false);return;}router.push(`/civilian/characters/${data.character.id}`);router.refresh();}
 return <div className="character-form-page"><div className="identity-notice"><BadgeCheck/><div><b>Automatic identity issuance</b><span>UltimateCAD will generate a permanent community-prefixed State ID when this character is created. The number will never be reused.</span></div></div>{error&&<div className="auth-error">{error}</div>}<form className="panel character-form" onSubmit={submit}><div className="form-section"><div><span className="eyebrow">Legal identity</span><h2>Personal information</h2></div><div className="character-form-grid"><label>First name *<input name="firstName" required maxLength={60}/></label><label>Middle name<input name="middleName" maxLength={60}/></label><label>Last name *<input name="lastName" required maxLength={60}/></label><label>Date of birth *<input type="date" name="dateOfBirth" required/></label><label>Gender<input name="gender" placeholder="Optional"/></label><label>Phone number<input name="phoneNumber" placeholder="555-0100"/></label><label className="full">Home address<input name="address" placeholder="Street, city or county"/></label><label>Occupation<input name="occupation"/></label><label>Photo URL<input type="url" name="photoUrl" placeholder="https://..."/></label></div></div><div className="form-section"><div><span className="eyebrow">Physical profile</span><h2>Description</h2></div><div className="character-form-grid"><label>Hair color<input name="hairColor"/></label><label>Eye color<input name="eyeColor"/></label><label>Height<input name="height" placeholder="Example: 5 ft 11 in"/></label><label>Weight<input name="weight" placeholder="Example: 180 lb"/></label></div></div><div className="form-section"><div><span className="eyebrow">Emergency details</span><h2>Emergency contact</h2></div><div className="character-form-grid"><label>Contact name<input name="emergencyContactName"/></label><label>Contact phone<input name="emergencyContactPhone"/></label></div></div><div className="character-form-actions"><Link className="button ghost" href="/civilian"><ArrowLeft size={16}/>Cancel</Link><button className="button primary" disabled={saving}>{saving?<LoaderCircle className="spin" size={17}/>:<Save size={17}/>} {saving?'Creating identity…':'Create character'}</button></div></form></div>}
