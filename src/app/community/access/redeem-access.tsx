"use client";
import { KeyRound } from "lucide-react";import { useState } from "react";
export default function RedeemAccess(){const[code,setCode]=useState('');const[msg,setMsg]=useState('');const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);const r=await fetch('/api/permissions/redeem',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({code})});const b=await r.json();setMsg(r.ok?'Access granted. Refresh the dashboard to see your department.':b.error);setLoading(false);}return <div className="access-redeem-card panel"><KeyRound size={34}/><span className="eyebrow">Permission key</span><h2>Unlock department access</h2><p>Enter the access key issued by your community leadership. Shared civilian modules remain available without a department key.</p><form onSubmit={submit}><input value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="UWRP-KEY-A1B2C3D4" required/><button className="button primary" disabled={loading}>{loading?'Checking…':'Redeem key'}</button></form>{msg&&<div className="status-banner">{msg}</div>}</div>}
