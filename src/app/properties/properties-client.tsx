"use client"; import {useState} from "react"; import {useRouter} from "next/navigation"; import styles from "./properties.module.css";
export default function C({character,items}:any){const r=useRouter(),[open,setOpen]=useState(false),[msg,setMsg]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),res=await fetch('/api/properties',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({characterId:character.id,propertyType:f.get('propertyType'),address:f.get('address'),unitNumber:f.get('unitNumber'),purchasePrice:f.get('purchasePrice'),garageSpaces:f.get('garageSpaces'),notes:f.get('notes')})}),b=await res.json();if(!res.ok)return setMsg(b.error);setOpen(false);r.refresh()}return <div className={styles.page}><section className={styles.hero}><div><span>Property registry</span><h2>{items.length} properties</h2><p>Homes, apartments, garages, commercial spaces and land.</p></div><button onClick={()=>setOpen(!open)}>Register property</button></section>{msg&&<div className={styles.message}>{msg}</div>}{open&&<form className={styles.form} onSubmit={submit}><div className={styles.formGrid}><label>Type<select name="propertyType"><option value="house">House</option><option value="apartment">Apartment</option><option value="commercial">Commercial</option><option value="garage">Garage</option><option value="land">Land</option></select></label><label>Address<input name="address" required/></label><label>Unit<input name="unitNumber"/></label><label>Purchase price<input name="purchasePrice" type="number" min="0"/></label><label>Garage spaces<input name="garageSpaces" type="number" min="0" defaultValue="0"/></label><label className={styles.full}>Notes<textarea name="notes"/></label></div><button>Register</button></form>}<section className={styles.grid}>{items.length?items.map((x:any)=><article className={styles.card} key={x.id}><code>{x.property_number}</code><h3>{x.address}</h3><p>{x.property_type} {x.unit_number?`· Unit ${x.unit_number}`:''}</p><dl><div><dt>Value</dt><dd>${Number(x.assessed_value||0).toLocaleString()}</dd></div><div><dt>Garages</dt><dd>{x.garage_spaces}</dd></div></dl></article>):<div className={styles.empty}>No registered properties.</div>}</section></div>}
